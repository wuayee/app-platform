namespace DataBus.Common;

enum MessageType:byte {
    Unknown = 0,
    HeartBeat = 1,
    ApplyMemory = 2,
    ApplyPermission = 3,
    ReleasePermission = 4,
    ReleaseMemory = 5
}

// 修改此 table 请务必与 DataBus 开发组人员确认
table MessageHeader {
    type: MessageType = 0;
    size: uint = 0;
}

root_type MessageHeader;