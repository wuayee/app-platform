namespace DataBus.Common;

enum ErrorType : byte {
  None = 0,
  NotConnectedToDataBus = 1, // 仅SDK使用
  DataBusDisconnected = 2, // 仅SDK使用
  OutOfMemory = 3,
  PermissionDenied = 4,
  Timeout = 5,
  IllegalMessageHeader = 6,
  IllegalMessageBody = 7,
  MallocFailed = 8,
  KeyNotFound = 9,
  MemoryNotFound = 10,
  MemoryReadError = 11,
  MemoryWriteError = 12,
  UnknownPermissionType = 13,
  DuplicatePermitApplication = 14,
  IllegalStateForPermitRelease = 15,
  IOOutOfBounds = 16,
  UnknownError = 17
}

// 修改此 Enum 请务必与 DataBus 开发组人员确认
enum PermissionType : byte {
  None = 0,
  Read = 1,
  Write = 2
}